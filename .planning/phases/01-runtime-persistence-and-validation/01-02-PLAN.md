---
phase: 01-runtime-persistence-and-validation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - package.json
  - vitest.config.ts
  - packages/core/package.json
  - packages/core/src/runtime/service.test.ts
  - apps/api/package.json
  - apps/api/src/app.ts
  - apps/api/src/index.ts
  - apps/api/src/index.test.ts
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
  - QUAL-03
user_setup: []
must_haves:
  truths:
    - Runtime lifecycle regressions are covered by automated tests.
    - API routes are verified without binding a real network port.
    - Root-level verification commands can run the phase test suite consistently on Node 20.
  artifacts:
    - vitest.config.ts exists and runs workspace tests.
    - packages/core/src/runtime/service.test.ts exists with lifecycle coverage.
    - apps/api/src/index.test.ts or equivalent route test file exists with Fastify injection coverage.
  key_links:
    - package.json exposes an `npm run test` command for the full suite.
    - apps/api/src/index.ts delegates app construction to a testable `buildApp()` helper.
---

<objective>
Install and wire the first automated verification baseline for the runtime and API.

Purpose: make Phase 1 changes safe to iterate on by replacing manual-only regression checking with repeatable commands.
Output: Vitest config, runtime lifecycle tests, API contract tests, and root test scripts.
</objective>

<execution_context>
@/Users/junxi/Desktop/work/agent-foundry/.codex/get-shit-done/workflows/execute-plan.md
@/Users/junxi/Desktop/work/agent-foundry/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-runtime-persistence-and-validation/01-RESEARCH.md
@.planning/phases/01-runtime-persistence-and-validation/01-VALIDATION.md
@package.json
@packages/core/package.json
@packages/core/src/runtime/service.ts
@apps/api/package.json
@apps/api/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest and add root verification commands</name>
  <files>package.json, vitest.config.ts, packages/core/package.json, apps/api/package.json</files>
  <read_first>package.json, packages/core/package.json, apps/api/package.json, tsconfig.base.json, .planning/phases/01-runtime-persistence-and-validation/01-VALIDATION.md</read_first>
  <action>Add Vitest as a root dev dependency and create `vitest.config.ts` configured for this npm workspace repo. Add root scripts `test` and `test:watch` to `package.json`, both wrapped through `bash scripts/with-node20.sh`, with `test` running Vitest in non-watch mode. Keep existing build/typecheck scripts intact. Only add per-workspace script changes if they are needed for isolated runs.</action>
  <verify>rg -n "\"test\"|\"test:watch\"" package.json && test -f vitest.config.ts && rg -n "vitest" package.json vitest.config.ts</verify>
  <acceptance_criteria>
    - `package.json` contains a root `"test"` script
    - `package.json` contains a root `"test:watch"` script
    - `vitest.config.ts` exists
    - `package.json` contains `"vitest"` in `devDependencies`
  </acceptance_criteria>
  <done>Vitest is installed and the repo has a single root test entrypoint.</done>
</task>

<task type="auto">
  <name>Task 2: Add runtime lifecycle tests in core</name>
  <files>packages/core/src/runtime/service.test.ts, packages/core/src/runtime/service.ts, packages/core/src/runtime/registry.ts, packages/core/src/adapters/in-memory.ts</files>
  <read_first>packages/core/src/runtime/service.ts, packages/core/src/runtime/types.ts, packages/core/src/runtime/registry.ts, packages/core/src/adapters/in-memory.ts, packages/core/src/packages/perfume/package.ts</read_first>
  <action>Create `packages/core/src/runtime/service.test.ts` covering at minimum: task creation enters planning flow, planner approval pauses work when required, approval submission resumes execution, executor low-confidence approval path can pause, and successful completion persists result/evidence state. Use fake or in-memory stores where appropriate, but assert against real `AgentRuntimeService` behavior rather than isolated helper functions.</action>
  <verify>bash scripts/with-node20.sh npx vitest run packages/core/src/runtime/service.test.ts</verify>
  <acceptance_criteria>
    - `packages/core/src/runtime/service.test.ts` contains tests for `createTask`
    - `packages/core/src/runtime/service.test.ts` contains tests for `submitApproval`
    - `packages/core/src/runtime/service.test.ts` contains an assertion involving `awaiting_approval`
    - `bash scripts/with-node20.sh npx vitest run packages/core/src/runtime/service.test.ts` exits 0
  </acceptance_criteria>
  <done>Core runtime lifecycle is protected by automated tests.</done>
</task>

<task type="auto">
  <name>Task 3: Extract testable Fastify app construction and add route tests</name>
  <files>apps/api/src/app.ts, apps/api/src/index.ts, apps/api/src/index.test.ts</files>
  <read_first>apps/api/src/index.ts, packages/shared/src/index.ts, packages/core/src/index.ts, .planning/phases/01-runtime-persistence-and-validation/01-RESEARCH.md</read_first>
  <action>Create a `buildApp()` helper in `apps/api/src/app.ts` or equivalent that registers CORS and all current routes, returning a Fastify instance suitable for `app.inject()`. Update `apps/api/src/index.ts` so the executable entrypoint just creates the app and calls `listen()`. Add `apps/api/src/index.test.ts` covering `/health`, `/packages`, `POST /tasks`, `GET /tasks/:taskId`, and approval error handling using Fastify injection rather than a live server.</action>
  <verify>bash scripts/with-node20.sh npx vitest run apps/api/src/index.test.ts</verify>
  <acceptance_criteria>
    - `apps/api/src/app.ts` contains `buildApp`
    - `apps/api/src/index.ts` imports `buildApp`
    - `apps/api/src/index.test.ts` contains `inject(`
    - `apps/api/src/index.test.ts` contains tests for `/health` and `/tasks`
    - `bash scripts/with-node20.sh npx vitest run apps/api/src/index.test.ts` exits 0
  </acceptance_criteria>
  <done>API contract tests run without starting a real HTTP server.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bash scripts/with-node20.sh npm run test`
- [ ] `bash scripts/with-node20.sh npm run typecheck`
- [ ] `rg -n "\"test\"|buildApp|inject\\(" package.json vitest.config.ts apps/api/src/app.ts apps/api/src/index.ts apps/api/src/index.test.ts packages/core/src/runtime/service.test.ts`
</verification>

<success_criteria>

- All tasks completed
- Root test commands exist and run
- Runtime lifecycle is covered by automated tests
- API routes are covered by injection-based tests
- Test execution works on the project's Node 20 path
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-persistence-and-validation/01-02-SUMMARY.md`
</output>
