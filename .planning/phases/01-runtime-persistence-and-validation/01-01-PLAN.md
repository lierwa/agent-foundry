---
phase: 01-runtime-persistence-and-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/package.json
  - packages/core/src/runtime/types.ts
  - packages/core/src/runtime/bootstrap.ts
  - packages/core/src/adapters/postgres.ts
  - packages/core/src/adapters/redis.ts
  - packages/core/src/index.ts
  - apps/api/src/index.ts
  - apps/api/.env.example
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
  - PLAT-03
user_setup: []
must_haves:
  truths:
    - API bootstrap can select durable or in-memory stores from explicit configuration.
    - Task state can be written and read back through a Postgres-backed store implementation.
    - Memory records can be appended and listed through a Redis-backed store implementation.
  artifacts:
    - packages/core/src/runtime/bootstrap.ts exists with runtime store selection logic.
    - packages/core/src/adapters/postgres.ts exists with a TaskStore implementation.
    - packages/core/src/adapters/redis.ts exists with a MemoryStore implementation.
    - apps/api/.env.example documents durable runtime environment variables.
  key_links:
    - apps/api/src/index.ts uses the runtime bootstrap instead of instantiating in-memory stores directly.
    - packages/core/src/index.ts exports the new bootstrap and durable adapters for reuse.
---

<objective>
Build the durable runtime storage foundation without changing the runtime's public behavior.

Purpose: move persistence behind the existing interfaces so the runtime can survive process restarts and later scale beyond a single process.
Output: Postgres-backed task store, Redis-backed memory store, and configuration-driven runtime bootstrap wired into the API.
</objective>

<execution_context>
@/Users/junxi/Desktop/work/agent-foundry/.codex/get-shit-done/workflows/execute-plan.md
@/Users/junxi/Desktop/work/agent-foundry/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-runtime-persistence-and-validation/01-RESEARCH.md
@packages/core/src/runtime/types.ts
@packages/core/src/runtime/service.ts
@packages/core/src/adapters/in-memory.ts
@packages/core/src/index.ts
@apps/api/src/index.ts
@apps/api/.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add durable storage dependencies and adapter modules</name>
  <files>packages/core/package.json, packages/core/src/adapters/postgres.ts, packages/core/src/adapters/redis.ts, packages/core/src/index.ts</files>
  <read_first>packages/core/package.json, packages/core/src/runtime/types.ts, packages/core/src/adapters/in-memory.ts, packages/core/src/index.ts</read_first>
  <action>Add `pg` and `redis` to `packages/core/package.json`. Create `packages/core/src/adapters/postgres.ts` exporting a `PostgresTaskStore` that implements `TaskStore` with `create`, `update`, `get`, and `list`, storing canonical task records keyed by `task_id` and ordering list output by `created_at DESC`. Create `packages/core/src/adapters/redis.ts` exporting a `RedisMemoryStore` that implements `MemoryStore` using a Redis key prefix like `agent-foundry:memory:{taskId}` and serializes records as JSON. Update `packages/core/src/index.ts` to export both new adapter modules.</action>
  <verify>rg -n "pg|redis" packages/core/package.json && rg -n "class PostgresTaskStore|class RedisMemoryStore" packages/core/src/adapters/postgres.ts packages/core/src/adapters/redis.ts && rg -n "postgres|redis" packages/core/src/index.ts</verify>
  <acceptance_criteria>
    - `packages/core/package.json` contains `"pg"` and `"redis"` in `dependencies`
    - `packages/core/src/adapters/postgres.ts` contains `class PostgresTaskStore`
    - `packages/core/src/adapters/redis.ts` contains `class RedisMemoryStore`
    - `packages/core/src/index.ts` re-exports `./adapters/postgres.js` and `./adapters/redis.js`
  </acceptance_criteria>
  <done>Durable adapter modules exist and are exported from the core package.</done>
</task>

<task type="auto">
  <name>Task 2: Add runtime bootstrap for store selection</name>
  <files>packages/core/src/runtime/bootstrap.ts, packages/core/src/index.ts, apps/api/src/index.ts, apps/api/.env.example</files>
  <read_first>packages/core/src/runtime/types.ts, packages/core/src/runtime/service.ts, packages/core/src/adapters/in-memory.ts, apps/api/src/index.ts, apps/api/.env.example</read_first>
  <action>Create `packages/core/src/runtime/bootstrap.ts` with a `createRuntimeServices` or similarly named factory that reads explicit env/config values such as `AGENT_FOUNDRY_STORE_MODE`, `DATABASE_URL`, and `REDIS_URL`, returning an `AgentRuntimeService` with either in-memory adapters or durable adapters. Update `apps/api/src/index.ts` to use this bootstrap instead of constructing `InMemoryTaskStore` and `InMemoryMemoryStore` inline. Expand `apps/api/.env.example` to document `AGENT_FOUNDRY_STORE_MODE=in-memory|durable`, `DATABASE_URL`, and `REDIS_URL` with local defaults matching `infra/docker-compose.yml`.</action>
  <verify>rg -n "AGENT_FOUNDRY_STORE_MODE|DATABASE_URL|REDIS_URL" packages/core/src/runtime/bootstrap.ts apps/api/src/index.ts apps/api/.env.example</verify>
  <acceptance_criteria>
    - `packages/core/src/runtime/bootstrap.ts` contains `AGENT_FOUNDRY_STORE_MODE`
    - `apps/api/src/index.ts` no longer imports `InMemoryTaskStore` or `InMemoryMemoryStore` directly
    - `apps/api/src/index.ts` imports the runtime bootstrap helper from `@agent-foundry/core`
    - `apps/api/.env.example` contains `DATABASE_URL=` and `REDIS_URL=`
  </acceptance_criteria>
  <done>API bootstrap selects runtime storage mode from configuration and env examples document the durable path.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve compatibility with existing runtime contracts</name>
  <files>packages/core/src/runtime/types.ts, packages/core/src/runtime/service.ts, packages/core/src/adapters/postgres.ts, packages/core/src/adapters/redis.ts</files>
  <read_first>packages/core/src/runtime/types.ts, packages/core/src/runtime/service.ts, packages/shared/src/index.ts, packages/core/src/adapters/postgres.ts, packages/core/src/adapters/redis.ts</read_first>
  <action>Keep `TaskStore` and `MemoryStore` method signatures unchanged. If durable adapters need serialization helpers or record mapping, add them inside the new adapter modules rather than changing `AgentRuntimeService` call sites. Only update `packages/core/src/runtime/types.ts` when a shared helper type is necessary, and keep `AgentRuntimeService` interacting with the same interface methods: `create`, `update`, `get`, `list`, `append`, and `listByTask`.</action>
  <verify>rg -n "interface TaskStore|interface MemoryStore" packages/core/src/runtime/types.ts && rg -n "\\.create\\(|\\.update\\(|\\.get\\(|\\.list\\(|\\.append\\(|listByTask" packages/core/src/runtime/service.ts</verify>
  <acceptance_criteria>
    - `packages/core/src/runtime/types.ts` still contains `interface TaskStore` with `create`, `update`, `get`, and `list`
    - `packages/core/src/runtime/types.ts` still contains `interface MemoryStore` with `append` and `listByTask`
    - `packages/core/src/runtime/service.ts` continues using the interface methods rather than branching on concrete adapter classes
  </acceptance_criteria>
  <done>Durable storage is added without breaking the runtime service contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bash scripts/with-node20.sh npm --workspace @agent-foundry/core run typecheck`
- [ ] `bash scripts/with-node20.sh npm --workspace @agent-foundry/api run typecheck`
- [ ] `rg -n "AGENT_FOUNDRY_STORE_MODE|DATABASE_URL|REDIS_URL" packages/core/src/runtime/bootstrap.ts apps/api/src/index.ts apps/api/.env.example`
</verification>

<success_criteria>

- All tasks completed
- Durable adapters exist and are exported
- API bootstrap no longer hardcodes in-memory stores
- Env documentation covers durable local mode
- No runtime interface breakage introduced
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-persistence-and-validation/01-01-SUMMARY.md`
</output>
