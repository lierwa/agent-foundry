---
phase: 01-runtime-persistence-and-validation
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - README.md
  - apps/api/.env.example
  - scripts/dev-doctor.sh
  - package.json
  - .planning/STATE.md
autonomous: true
requirements:
  - PLAT-03
  - QUAL-03
user_setup: []
must_haves:
  truths:
    - Contributors can discover how to run durable mode and the test suite from repo docs and scripts.
    - Local environment checks fail clearly when Postgres/Redis prerequisites for durable mode are missing.
  artifacts:
    - README.md documents durable runtime mode and verification commands.
    - scripts/dev-doctor.sh checks the durable-mode prerequisites and reports missing dependencies clearly.
  key_links:
    - README commands match the actual scripts in package.json.
    - .env examples and docs refer to the same environment variable names introduced in Plan 01.
---

<objective>
Finish Phase 1 by making the durable path and verification workflow discoverable for contributors.

Purpose: reduce configuration drift after persistence and tests land, and ensure contributors can actually use what Phase 1 adds.
Output: updated docs, stronger dev doctor guidance, and aligned verification commands.
</objective>

<execution_context>
@/Users/junxi/Desktop/work/agent-foundry/.codex/get-shit-done/workflows/execute-plan.md
@/Users/junxi/Desktop/work/agent-foundry/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@README.md
@apps/api/.env.example
@scripts/dev-doctor.sh
@package.json
@.planning/phases/01-runtime-persistence-and-validation/01-01-PLAN.md
@.planning/phases/01-runtime-persistence-and-validation/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document durable mode and verification workflow</name>
  <files>README.md, apps/api/.env.example, package.json</files>
  <read_first>README.md, apps/api/.env.example, package.json, .planning/phases/01-runtime-persistence-and-validation/01-01-PLAN.md, .planning/phases/01-runtime-persistence-and-validation/01-02-PLAN.md</read_first>
  <action>Update `README.md` so it explains both in-memory and durable runtime modes, including the exact environment variables `AGENT_FOUNDRY_STORE_MODE`, `DATABASE_URL`, and `REDIS_URL`, plus the exact verification command `npm run test`. Keep the quick-start section coherent with the new reality instead of leaving durable mode implied only in architecture text. Ensure `apps/api/.env.example` and README use the same variable names and example URLs.</action>
  <verify>rg -n "AGENT_FOUNDRY_STORE_MODE|DATABASE_URL|REDIS_URL|npm run test" README.md apps/api/.env.example</verify>
  <acceptance_criteria>
    - `README.md` contains `AGENT_FOUNDRY_STORE_MODE`
    - `README.md` contains `npm run test`
    - `apps/api/.env.example` contains `DATABASE_URL=`
    - `apps/api/.env.example` contains `REDIS_URL=`
  </acceptance_criteria>
  <done>Docs and env examples describe the same durable-mode workflow and verification commands.</done>
</task>

<task type="auto">
  <name>Task 2: Expand dev doctor for durable prerequisites</name>
  <files>scripts/dev-doctor.sh</files>
  <read_first>scripts/dev-doctor.sh, infra/docker-compose.yml, README.md</read_first>
  <action>Extend `scripts/dev-doctor.sh` so it still checks Node 20+, but also reports whether durable-mode prerequisites are usable: Docker reachable, `psql` available when Postgres troubleshooting is needed, `redis-cli` availability, and whether env guidance for durable mode is likely satisfied. Keep the script non-destructive and fast. If exact env vars are missing, print actionable messages rather than silently passing.</action>
  <verify>rg -n "DATABASE_URL|REDIS_URL|docker engine|psql|redis-cli" scripts/dev-doctor.sh</verify>
  <acceptance_criteria>
    - `scripts/dev-doctor.sh` contains `DATABASE_URL` or `REDIS_URL` checks/messages
    - `scripts/dev-doctor.sh` still contains `Node 20+ is required`
    - `scripts/dev-doctor.sh` still checks Docker reachability
  </acceptance_criteria>
  <done>Dev doctor helps contributors diagnose durable-mode readiness.</done>
</task>

<task type="auto">
  <name>Task 3: Align project state after Phase 1 implementation path is documented</name>
  <files>.planning/STATE.md</files>
  <read_first>.planning/STATE.md, .planning/PROJECT.md, .planning/ROADMAP.md</read_first>
  <action>After the implementation and docs changes land, update `.planning/STATE.md` so current position, blockers, and continuity notes reflect that Phase 1 execution is underway or complete. Keep the file under its intended digest size and update only the sections that should change from the new durable/test baseline.</action>
  <verify>rg -n "Phase:|Status:|Last activity:|Blockers/Concerns" .planning/STATE.md</verify>
  <acceptance_criteria>
    - `.planning/STATE.md` still contains `Project Reference`, `Current Position`, and `Session Continuity`
    - `.planning/STATE.md` reflects a post-implementation Phase 1 status rather than the pre-planning baseline
  </acceptance_criteria>
  <done>Project state matches the implemented Phase 1 baseline.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bash scripts/with-node20.sh npm run doctor`
- [ ] `bash scripts/with-node20.sh npm run test`
- [ ] `rg -n "AGENT_FOUNDRY_STORE_MODE|DATABASE_URL|REDIS_URL|npm run test" README.md apps/api/.env.example scripts/dev-doctor.sh`
</verification>

<success_criteria>

- All tasks completed
- Durable mode is documented from env setup through verification
- Dev doctor reports durable prerequisites clearly
- Project state is updated to match the completed work
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-persistence-and-validation/01-03-SUMMARY.md`
</output>
